# 任务
    获取头条街拍 只获取纯图片的页面，并将图片下载下来

# 进程：
    是为了解决多任务处理 并发

# 程序和进程之间的关系
    程序运行起来之后就是进程
    没有运行的时候就是代码
    进程的执行需要环境支持

# 进程的特点：
    进程和进程之间 完全独立 分别有自己的资源
    多个进程是同时执行 没有顺序

    需要你去查询 进程的概念  线程  进程和线程的区别  优缺点

    fork()
# multiprocessing
    创建
    Process(target,name,args,kwargs)

    启动
    strat()

    # 等待 join  阻塞

    # 检测进程是否执行结束 is_alive

    # 强制终止进程terminate()
